# Packet Tracer Simulation (Release 1)

This simulation uses a Temperature Monitor device in Packet Tracer.
The sensor runs a Python script that generates random temperature values
and sends them via UDP to a PC listener.

No real hardware is used. This satisfies the simulated sensor requirement.
